# Calculation engine

Work Package 1 will document the independently verified equations, typed input
and result contracts, golden cases, unit conversions, and presentation-only
rounding rules. ADR-003 defines the canonical units and pure-library boundary
that apply before that implementation begins.
